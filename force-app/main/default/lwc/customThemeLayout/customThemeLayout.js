import { LightningElement } from "lwc";

/**
 * @slot header this is the header slot
 * @slot footer this is the footer slot
 * @slot default this is the default slot
 * DOCS - https://developer.salesforce.com/docs/atlas.en-us.exp_cloud_lwr.meta/exp_cloud_lwr/get_started_layout.htm
 * A anotação JSDoc contendo '@slot' seguido do nome da região é necessário para que a plataforma saiba quais slots
 * (ou regiões no Experience Builder) estão expostos para o seu componente de layout de página. Por
 */
export default class CustomThemeLayout extends LightningElement {}
